# Revisão java intermediario

Prova:

1. O que é um objeto ? 

Tudo o que pode ser instanciado a partir de uma classe vira um objeto na memória do computador

2. O que é um atributo de um objeto?

R: caracteristica os dados do objeto.

3. O que é poo?

R:A Programação Orientada a Objetos (POO) é um paradigma(Um paradigma de desenvolvimento define o conjunto de crenças, conceitos e metodologias que guiam a forma como os profissionais abordam a criação de algo em desenvolvimento que organiza o código em "objetos" — que combinam dados e comportamentos. Ela é amplamente utilizada porque facilita a criação de sistemas complexos, promove a reutilização de código e torna a manutenção muito mais eficiente


4. O que é herança?

R:Usar caracteristicas de um class mãe em outras classes filhas para assim poupar codigo e agilizar trabalho.
ou seja todos os atributos e metodos da classe

5. Qual a palavra chave pra extender uma classes em java 

R: extends

6. O que é superclasse e subclasse e qual analogia pode fazer ?

R:  um superclassse e classe que distribui as caracteristicas e a subclasse e a que recebe
uma analogia seria mae e filho


7. Quais os três pilares da Programação orientada a objeto?

R: herança, polimorfismo e encapsulamento

8. O que é polimorfismo?


Criar uma função unica (trecho de código[algoritmos]) que pode ser utilizada em diferentes classes com diferentes caracteristicas.

Polimorfismo permite que objetos de diferentes classes sejam tratados como objetos de uma classe base comum, o que facilita a criação de código flexível e reutilizável.

os tipos de polimorfimso são:

estático(sobrecarga/overload) - varios metodos possuem o mesmo nome mas recebe parametros diferentes.

dinamico (sobrescrita) - uma classe redefine um método herdado da superclasse para dar a ele um comportamento específico.

9. O que sõa métodos e parãmetros?

R: método e o conjunto de passos a serem seguidos por um sub programa podendo ser um procedimento ou função e
parâmetro variáveis declaradas entre os parênteses de um método que permitem receber dados externos para processamento.

10. Quais os tipos de métodos presentes em java?

R: 1) Void -  Não precisa retornar nada pra gente (procedimento)
 e  2) Return -  Precisa retornar um valor pra gente (função)(return) e tem que ter um tipo de dado específico(qualquer tipo)
outra coisa que podemos usar parâmetros( as declarações dentro dos parenteses dos métodos) ou seja o método vai acontecer se você me passar o valor do parãmetro



11. O que são interfaces?  

R: Uma interface em Java funciona como um contrato que define um conjunto de métodos que uma classe deve obrigatoriamente implementar


12. O que são constutores

R:Em Java, um construtor é um bloco de código especial usado para criar e inicializar um objeto de uma classe

13. Quais os tipos de construtores? 

R:  1) NoArgs Constructor

Um construtor que vem vazio , não necessariamente recebe argumento nenhum.

2) AllArgs Constructor

um constructor que é preenchido como todos os atributos de classe - com argumentos.


14. O que é um argumento? 

R: é o valor real passado durante a chamada do método.

15. O que são classes abstratas

R:Uma classe abstrata é um modelo em programação orientada a objetos que não pode ser instanciado diretamente e serve apenas como base para outras classes.Elas funcionam como um rascunho ou uma planta baixa: definem regras gerais e comportamentos comuns, mas deixam os detalhes específicos para serem preenchidos pelas classes filhas (subclasses)

16. Como usar classes abstratas ?

17. Qual a diferença de classes abstratas e interfaces

18. o que é Herança multípla? 

19. O que é overload?

20. O que é sobrescrita de método?

21. Diferença entre sobrescrita e sobrecarga de método.

22. O que é um enum ?

23. Pra que server o Override? 

24. o que é e qual a diferença entre referência de mémoria e valor em mémoria.

25. O que é a palavra chave final e o que ela faz ?

26. O que acontece em uma classe final? 

27. O que acontece em um método e atributo final

28. o que é encapsulamento

29. Qual a diferença da palavra chave final e privada.

30. O que são getters e setters 

31. Quais os problemas que o encapsulamento resolve?

32. Como usar getters e setter em um enum ?

33. O que é lista ?

34. Qual a diferença de uma lista e um array ?

35. Como usar listas? 

36. O que são genérics e como funcionam? 

37. O que é um records e como funciona ?

38. Como usar records e generics ?

39. O que é uma stack e como funciona/ 

40. Como usar uma stack? 

41. Diferença de array, list e stack

42. O que é um queue e como funciona? 

43. O que é um linkedList e como funciona?

44. Qual a diferença de um arraylist e um linkedlist

45. O que são colections?

46. O que é SET e qual as suas implementações.

47. O que faz cada implementação do SET?

48. Como refatorar código

49. O que é a métodologia Extreme Go Horse. 

50. Como referenciar o endereço de mémoria de uma memoria de  um parâmetro com uma variavel da  classe?





