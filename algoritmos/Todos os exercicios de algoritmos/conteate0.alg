algoritmo "conte até 0"
var
      contador:inteiro
inicio
      contador <- 10
      Enquanto (contador >= 0 ) faca
            Escreval(contador)
            contador <- contador - 1   // Execute com timer
      Fimenquanto
      Escreval("Terminei de contar. ")
fimalgoritmo
